class Input{constructor(canvas){this.keys={};this.touch={active:false,sx:0,sy:0,x:0,y:0};this.skill=false;addEventListener('keydown',e=>{this.keys[e.key.toLowerCase()]=true;if(e.code==='Space')this.skill=true});addEventListener('keyup',e=>this.keys[e.key.toLowerCase()]=false);canvas.addEventListener('pointerdown',e=>{if(Game?.mode!=='playing'||e.clientX>innerWidth*.72)return;this.touch={active:true,sx:e.clientX,sy:e.clientY,x:e.clientX,y:e.clientY};canvas.setPointerCapture?.(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(this.touch.active){this.touch.x=e.clientX;this.touch.y=e.clientY}});const end=()=>this.touch.active=false;canvas.addEventListener('pointerup',end);canvas.addEventListener('pointercancel',end)}move(){let x=(this.keys.d||this.keys.arrowright?1:0)-(this.keys.a||this.keys.arrowleft?1:0),y=(this.keys.s||this.keys.arrowdown?1:0)-(this.keys.w||this.keys.arrowup?1:0);if(this.touch.active&&Save.data.settings.touchMode){const dx=this.touch.x-this.touch.sx,dy=this.touch.y-this.touch.sy,l=Math.hypot(dx,dy);if(l>8){x=dx/Math.max(35,l);y=dy/Math.max(35,l)}}const l=Math.hypot(x,y);return l>1?{x:x/l,y:y/l}:{x,y}}consumeSkill(){const s=this.skill;this.skill=false;return s}}
