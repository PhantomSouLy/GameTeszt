# CHERRIFT v0.3.1 Loading + HUD + Zoom Fix

## Fixed
- Added a proper loading screen between World Select Play and stage start.
- Skill button / HUD are hidden while the stage is loading.
- Stage HUD is now compact: no huge glass card covering HP and gameplay.
- Stage info shows only small `World x-y` and kill objective.
- Mobile fullscreen and non-fullscreen zoom are normalized to a middle-ground scale.
- PC fullscreen / non-fullscreen also gets a consistent broader zoom.
- World Select flow remains: main Play -> World Select -> Play.

## Notes
- PC main menu layout is preserved.
- This patch should be copied over the current v0.3.1 clean fix.
